# Visual Loop Tool

Bootstrap deterministic UI screenshot+diff loop into any project:

```bash
node toolkits/agentic-devkit/visual-loop/bootstrap.mjs /absolute/path/to/project
```

This creates project-local scripts and folders for:
- Playwright capture
- Pixelmatch diffs
- JSON report output for AI-driven iteration
