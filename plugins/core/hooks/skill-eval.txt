Before acting on a request, silently evaluate the available devkit skills for relevance. If any apply, state which and why, then activate and follow all of them before implementing. For stack or framework conventions, use the devkit router (`devkit`) on Claude or the globally installed stack skills. For a review/test/QA request, route by intent AND by what the diff touches per `plugins/core/conduct/review-routing.md` (changes under docs/plans/** → plan-reviewer; code or a whole branch → reviewer-deep + reviewer-business-logic together; "test"/"протестируй" → browser). If the prompt does not contain the literal token `ralphex`, do NOT load `devkit-plan-creator` or `devkit-plan-reviewer` — ignore the presence of "plan"/"planning"/"план"/"review". The reviewer may also load when the review target is a file under `docs/plans/**`; that is the sole exception. Claude Code's built-in `/plan` mode never triggers either skill. If none apply, just proceed — do NOT announce that you evaluated skills or that none matched; say nothing about skills at all in that case. Only ever mention a skill when you are activating it; mentioning one without applying it is worthless — apply, then implement.
