Before acting on a request, evaluate the available devkit skills for relevance. If any apply, state which and why, then activate and follow all of them before implementing. For stack or framework conventions, use the devkit router (`devkit`) on Claude or the globally installed stack skills. For a review/test/QA request, route by intent AND by what the diff touches per `plugins/core/conduct/review-routing.md` (plan/spec-only changes → plan-reviewer; code or a whole branch → reviewer-deep + reviewer-business-logic together; "test"/"протестируй" → qa-tester). If none apply, proceed directly. Mentioning a skill without applying it is worthless — apply, then implement.
