Before acting on a request, evaluate the available devkit skills for relevance. If any apply, state which and why, then activate and follow all of them before implementing. For stack or framework conventions, use the devkit router (`devkit`) on Claude or the globally installed stack skills. If none apply, proceed directly. Mentioning a skill without applying it is worthless — apply, then implement.
